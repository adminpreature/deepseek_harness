# DeepSeek Harness UI 需求规范
## OneSeven 品牌视觉改造方案

**版本**：1.0  
**日期**：2026-08-15  
**品牌方向**：OneSeven / 壹柒研究所  
**项目阶段**：规划与需求

---

## 一、产品画像与定位

### 1.1 产品概述

**DeepSeek Harness** 是一个开源的插件化 agent 开发框架，基于 Cordis 架构，为 AI 应用开发者、研究人员和技术团队提供完整的 agent 构建和调试环境。

### 1.2 产品画像

**主类型**：AI 创作 / 对话工作台  
**次类型**：运营管理 SaaS

**核心特征**：
- 与 AI agent 持续对话交互
- 查看执行日志和工具调用
- 管理插件配置和系统资源
- 调试 agent 行为和工作流

### 1.3 目标用户

- **AI 应用开发者**：构建和测试 AI agents
- **技术研究人员**：实验和优化 agent 行为
- **产品技术团队**：管理和部署 agent 系统

### 1.4 使用场景

- **持续工作**：开发者长时间停留在工作台
- **高频切换**：频繁切换会话、查看日志、修改配置
- **信息密度**：中到高密度，平衡可读性与效率

---

## 二、页面地图与信息架构

### 2.1 整体布局

采用**三栏工作台布局**（Sidebar + Center + Details）：

```
┌────────────┬────────────────────────┬────────────┐
│            │                        │            │
│  Sidebar   │   Conversation Area    │  Details   │
│  (可折叠)   │   (对话主区)            │  (可收起)   │
│            │                        │            │
└────────────┴────────────────────────┴────────────┘
```

### 2.2 页面类型与 Hero 判断

**所有页面均为 No Hero**（持续工作界面，无营销或介绍性内容）

| 页面/区域 | 页面类型 | Hero | 说明 |
|----------|---------|------|------|
| 对话工作台 | AI 创作工作台 | No Hero | 主要工作区 |
| 侧栏会话列表 | 运营管理 | No Hero | 资源浏览 |
| 设置界面 | 运营管理 | No Hero | 系统配置 |
| 详情面板 | 数据展示 | No Hero | 执行细节 |

### 2.3 页面结构

#### A. 侧栏（Sidebar）

**功能区块**：
1. **顶部**：Logo / 品牌区 + 折叠按钮
2. **新建会话**：快速启动新对话
3. **工作空间浏览**：会话列表（可滚动）
4. **页脚操作**：扩展功能入口
5. **设置入口**：系统配置访问

**响应式行为**：
- 桌面：展开态（默认宽度可拖拽调整）
- 窄视口：自动收起为轨道栏（56px），显示图标
- 支持手动展开/折叠

#### B. 对话主区（Center Column）

**核心组件**：
1. **对话视图（ChatView）**
   - 用户消息节点
   - AI 助手回复节点
   - 工具调用卡片
   - 执行状态指示器
   - 加载历史记录按钮
   - 滚动到底部按钮

2. **输入组合器（Composer）**
   - 文本输入区
   - 附件管理
   - 发送按钮

#### C. 详情栏（Details Column）

**面板类型**：
- 待办面板（TodoPanel）
- 审批面板（ApprovalPanel）
- 计划审查面板（PlanReviewPanel）
- 工作流运行面板（WorkflowRunPanel）

**交互行为**：
- 按需显示，默认隐藏
- 宽度可拖拽调整
- 切换会话时自动关闭

#### D. 设置界面

**设置分类**：
1. **通用设置**：外观、语言等
2. **模型设置**：AI 模型配置
3. **插件管理**：插件启用/禁用
4. **插件清单**：可用插件浏览

---

## 三、OneSeven 品牌视觉规范

### 3.1 品牌硬边界（强制遵守）

#### Logo 规范

- **使用 OneSeven 官方 Logo**：`.agents/skills/yiqi-ui-standard/assets/oneseven-logo.png`
- **浅色背景**：使用黑色 Logo
- **暗色背景**：使用白色反相 Logo
- **禁止行为**：拉伸、旋转、描边、渐变填充、发光、添加阴影

**应用位置**：
- 侧栏顶部品牌区
- 加载页面
- 设置页面

#### 品牌色彩

**品牌紫**：`#9A6ABA`

**使用场景**（克制使用）：
- 主要操作按钮（新建会话、发送等）
- 选中状态指示
- 焦点高亮
- 关键状态标记

**色彩角色**：
- 紫色用于点缀和强调，不是大面积主导色
- 同一产品内颜色角色保持稳定
- 明暗主题保持信息层级关系

#### 字体规范

**品牌字体**：**Alibaba PuHuiTi**（阿里巴巴普惠体）

**字重使用**：
- **200 (Thin)**：品牌特征字重，用于大标题、Hero 文案
- **400 (Regular)**：正文内容
- **500 (Medium)**：次级标题、强调文本
- **600 (SemiBold)**：按钮文字、导航标签

**WOFF2 文件位置**：`.agents/skills/yiqi-ui-standard/assets/fonts/`
- `AlibabaPuHuiTi-3-35-Thin.woff2`
- `AlibabaPuHuiTi-3-55-Regular.woff2`
- `AlibabaPuHuiTi-3-65-Medium.woff2`
- `AlibabaPuHuiTi-3-75-SemiBold.woff2`

**回退字体**：
```css
font-family: 'Alibaba PuHuiTi', -apple-system, BlinkMacSystemFont, 
  'Segoe UI', 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', 
  'Helvetica Neue', Helvetica, Arial, sans-serif;
```

### 3.2 排版角色范围

**标题层级**（浅色主题下的规范）：

| 角色 | 桌面字号 | 移动字号 | 字重 | 使用场景 |
|------|---------|---------|------|---------|
| Page Title | 28–32px | 24–28px | 200 (Thin) | 设置页标题 |
| Section Title | 22–24px | 20–22px | 500 (Medium) | 区域标题 |
| Card Title | 16–20px | 16–18px | 500 (Medium) | 对话节点标题、面板标题 |
| SaaS Page Title | 20–24px | 20–22px | 500 (Medium) | 侧栏顶部 |
| SaaS Panel Title | 16–18px | 16–18px | 500 (Medium) | 详情面板标题 |
| Body / Controls | 13–16px | 13–16px | 400 (Regular) | 正文、按钮、表单 |
| Caption | 12–13px | 12–13px | 400 (Regular) | 辅助信息、时间戳 |

**强制规则**：
- 每个视口内只能有一个 Page Title 级别标题
- 标题字号不超出角色范围
- 标题字号不随视口无限放大

### 3.3 卡片与圆角规范

**材质选择**：**实体材质**（用户明确请求）

**圆角规范**：
- **桌面**：36px 连续大圆角（Squircle / 连续曲率）
- **移动端**：28–36px，默认 32px

**实体材质属性**：
- 实体背景色
- 实体边框
- Solid shadow（无模糊）
- 无 backdrop-filter

**强制规则**：
- 所有独立卡片遵守统一圆角
- 同一页面不得混用不同圆角系统
- 不得退回控件级小圆角（如 8px、12px）

### 3.4 主题色调

**默认主题**：**浅色主题**（用户明确请求，OneSeven 默认为暗色的例外情况）

**浅色主题色彩系统**：

| 角色 | Token 示例 | 用途 |
|------|-----------|------|
| 背景基础 | `--dsw-static-neutral-00` | 页面底色 |
| 背景层级 1 | `--dsw-static-neutral-50` | 卡片背景 |
| 背景层级 2 | `--dsw-static-neutral-100` | 悬停背景 |
| 主要文本 | `--dsw-static-neutral-900` | 标题、正文 |
| 次要文本 | `--dsw-static-neutral-600` | 辅助信息 |
| 边框 | `--dsw-static-neutral-200` | 分割线、卡片边框 |
| 品牌主色 | `#9A6ABA` | 主要操作、选中态 |

**支持主题切换**：
- 浅色（默认）
- 暗色（可选）
- 跟随系统（可选）

---

## 四、组件状态与交互规范

### 4.1 按钮规范

#### 主要按钮（Primary Button）

**视觉**：
- 背景：品牌紫 `#9A6ABA`
- 文本：白色
- 圆角：8px
- 高度：32px（中）/ 40px（大）
- 内边距：16px 左右

**状态**：
- **默认**：品牌紫背景
- **悬停**：背景加深 10%
- **按下**：背景加深 20%
- **禁用**：50% 不透明度

**使用场景**：
- 新建会话
- 发送消息
- 确认操作

#### 次要按钮（Secondary Button）

**视觉**：
- 背景：透明
- 边框：1px solid 边框色
- 文本：主要文本色
- 圆角：8px

**使用场景**：
- 取消操作
- 次要功能入口

#### 图标按钮（Icon Button）

**视觉**：
- 背景：透明（悬停时显示背景）
- 图标：16px / 18px / 20px
- 点击区域：最小 32x32px

**使用场景**：
- 侧栏折叠按钮
- 滚动到底部
- 工具栏图标

### 4.2 卡片组件

#### 对话消息卡片

**用户消息**：
- 背景：次级背景色
- 边框：1px solid 边框色
- 圆角：12px
- 内边距：12px 16px
- 最大宽度：80%
- 对齐：右侧

**AI 回复卡片**：
- 背景：主背景色 / 卡片背景色
- 边框：1px solid 边框色（可选）
- 圆角：12px
- 内边距：16px 20px
- 宽度：100%

**工具调用卡片**：
- 背景：中性背景
- 边框：1px solid 边框色
- 圆角：12px
- 标题栏：工具名称 + 状态图标
- 内容区：可折叠展开
- 状态指示：
  - 执行中：品牌紫脉动动画
  - 成功：绿色指示
  - 失败：红色指示

### 4.3 列表与导航

#### 会话列表项

**视觉**：
- 高度：56px
- 内边距：12px 16px
- 圆角：8px（悬停/选中时）

**状态**：
- **默认**：透明背景
- **悬停**：次级背景色
- **选中**：品牌紫 10% 不透明度背景 + 左侧 3px 品牌紫边条

**内容结构**：
- 会话标题（1 行，超出省略）
- 最后消息预览（1 行，灰色）
- 时间戳（右上角，小字号）

#### 侧栏导航

**展开态**（宽度 240px–320px）：
- 显示完整文本标签
- 图标 + 文字

**收起态**（宽度 56px）：
- 仅显示图标
- Tooltip 提示文字

### 4.4 表单与输入

#### 文本输入框

**视觉**：
- 边框：1px solid 边框色
- 圆角：8px
- 内边距：8px 12px
- 最小高度：36px

**状态**：
- **默认**：边框色
- **聚焦**：品牌紫边框
- **错误**：红色边框
- **禁用**：50% 不透明度

#### 对话输入框（Composer）

**特殊处理**：
- 多行文本域
- 最小高度：48px
- 最大高度：200px（自动扩展）
- 附件预览区域
- 发送按钮（右下角）

---

## 五、动效与交互反馈

### 5.1 动效原则

**优先级**：及时、连续、可理解、可中断

**实现方式**：CSS transitions / animations（不强制要求弹簧动画）

### 5.2 标准动效

#### 过渡时长

```css
--ds-transition-duration-fast: 0.1s;   /* 小元素状态切换 */
--ds-transition-duration: 0.2s;        /* 标准过渡 */
--ds-transition-duration-slow: 0.3s;   /* 布局变化 */
```

#### 缓动曲线

```css
--ds-ease-in-out: cubic-bezier(0.4, 0, 0.2, 1);
```

#### 常见动效场景

| 场景 | 时长 | 缓动 | 属性 |
|------|------|------|------|
| 按钮悬停 | 0.1s | ease-in-out | background-color |
| 侧栏展开/收起 | 0.3s | ease-in-out | width, opacity |
| 卡片悬停 | 0.2s | ease-in-out | background-color, box-shadow |
| 消息发送 | 0.2s | ease-out | opacity, transform |
| 加载指示器 | 0.8s | linear | rotate (无限循环) |

### 5.3 状态反馈

**执行状态指示器**：
- **执行中**：品牌紫色脉动动画
- **成功**：绿色 ✓ 图标
- **失败**：红色 ✗ 图标 + 错误消息
- **文本标签**："Deep diving..." / "执行中..." + 计时器（超过 15 秒显示）

**加载状态**：
- 页面级：中央加载动画 + "Loading plugins..." 文案
- 组件级：Skeleton 占位或小型 spinner

---

## 六、响应式设计

### 6.1 断点定义

| 名称 | 宽度范围 | 布局行为 |
|------|---------|---------|
| Mobile | < 768px | 侧栏自动收起，单栏优先 |
| Tablet | 768px–1024px | 侧栏可手动展开/收起 |
| Desktop | > 1024px | 三栏完整布局 |

### 6.2 响应式规则

#### 侧栏响应

- **< 侧栏自动折叠阈值**：自动收起为 56px 轨道栏
- **≥ 侧栏自动折叠阈值**：展开为完整宽度
- 用户手动展开在窄视口下的覆盖行为：临时覆盖层

#### 字号响应

- 标题字号按角色范围在移动端缩小
- 正文字号保持 13px–16px 可读性
- 不强制响应式缩放所有文本

#### 布局响应

- **桌面**：三栏并列
- **平板**：中间栏 + 一个侧栏
- **移动**：单栏堆叠，通过导航切换

---

## 七、可访问性要求

### 7.1 色彩对比度

**WCAG AA 标准**：
- 正文文本：4.5:1
- 大文本（18px+ 或 14px+ 粗体）：3:1
- UI 组件和图形：3:1

**验证工具**：
- 使用 Chrome DevTools Contrast Ratio 检查器
- WebAIM Contrast Checker

### 7.2 键盘导航

**要求**：
- 所有交互元素可通过 Tab 访问
- 焦点状态清晰可见
- 支持 Enter/Space 激活按钮
- 支持 Esc 关闭弹窗/面板

**焦点指示器**：
- 外边框：2px solid 品牌紫
- 偏移：2px
- 圆角：继承元素圆角

### 7.3 ARIA 标签

**保持现有实现**：
- `aria-label`：按钮和图标的描述性标签
- `aria-pressed`：切换按钮状态
- `role="status"` + `aria-live="polite"`：动态状态更新
- `aria-hidden`：装饰性元素

---

## 八、实现清单与验收标准

### 8.1 品牌基础（强制）

- [ ] 替换 Logo 为 OneSeven 官方 Logo
- [ ] 应用 Alibaba PuHuiTi 字体（WOFF2 文件引入）
- [ ] 品牌紫 `#9A6ABA` 应用于主要操作和选中状态
- [ ] 所有独立卡片使用 36px 连续大圆角（桌面）
- [ ] 实体材质卡片（无玻璃模糊效果）

### 8.2 排版（强制）

- [ ] 标题字号符合角色范围
- [ ] 每个视口只有一个 Page Title
- [ ] 字重正确应用（200/400/500/600）
- [ ] 响应式字号调整

### 8.3 组件实现（推荐）

- [ ] 按钮状态完整（默认/悬停/按下/禁用）
- [ ] 对话消息卡片视觉差异化（用户 vs AI）
- [ ] 工具调用卡片状态指示清晰
- [ ] 会话列表选中态视觉突出
- [ ] 侧栏展开/收起动画流畅

### 8.4 交互反馈（推荐）

- [ ] 按钮悬停反馈及时（0.1s）
- [ ] 加载状态明确可见
- [ ] 执行状态指示器清晰
- [ ] 焦点状态易于辨识

### 8.5 响应式（推荐）

- [ ] 侧栏在窄视口自动收起
- [ ] 移动端字号适配
- [ ] 触摸目标最小 44x44px

### 8.6 可访问性（强制）

- [ ] 色彩对比度符合 WCAG AA
- [ ] 键盘导航完整
- [ ] ARIA 标签保留
- [ ] 焦点指示器可见

### 8.7 浏览器验收

**桌面浏览器测试**：
- [ ] Chrome（最新版）
- [ ] Firefox（最新版）
- [ ] Safari（最新版）
- [ ] Edge（最新版）

**移动浏览器测试**：
- [ ] iOS Safari
- [ ] Android Chrome

**验收项目**：
- 字体正确加载
- 布局无错位
- 交互反馈流畅
- 色彩对比度达标

---

## 九、实施计划

### 9.1 改造范围

**仅改造视觉层，不修改**：
- ✅ 业务逻辑
- ✅ 交互逻辑
- ✅ 组件结构
- ✅ 状态管理
- ✅ 数据流

**改造内容**：
- ✅ CSS 样式文件
- ✅ 主题 token 定义
- ✅ 字体引入
- ✅ Logo 资源
- ✅ 色彩变量

### 9.2 实施步骤

#### 阶段 1：基础设施（第 1–2 天）

1. **字体引入**
   - 复制 WOFF2 字体文件到项目
   - 更新 `@font-face` 声明
   - 更新 `--dsw-font-family` 变量

2. **Logo 替换**
   - 复制 OneSeven Logo 资源
   - 替换侧栏 Logo
   - 替换加载页 Logo

3. **主题 token 更新**
   - 更新 `design-platform.css` 色彩 token
   - 定义品牌紫变量
   - 定义浅色主题语义 token

#### 阶段 2：核心组件（第 3–5 天）

1. **按钮组件**
   - 更新主要按钮样式（品牌紫）
   - 更新次要按钮样式
   - 统一图标按钮尺寸

2. **卡片组件**
   - 更新对话消息卡片
   - 更新工具调用卡片
   - 统一圆角为 36px（桌面）

3. **侧栏组件**
   - 更新会话列表项样式
   - 更新选中态视觉
   - 优化展开/收起动画

#### 阶段 3：页面级优化（第 6–7 天）

1. **对话主区**
   - 优化消息流视觉层级
   - 更新加载状态样式
   - 优化滚动体验

2. **设置界面**
   - 更新表单样式
   - 优化布局间距
   - 统一组件视觉

3. **详情面板**
   - 更新面板头部样式
   - 优化内容区布局

#### 阶段 4：验收与修复（第 8 天）

1. **浏览器测试**
   - 桌面浏览器验收
   - 移动浏览器验收
   - 记录问题清单

2. **可访问性检查**
   - 对比度验证
   - 键盘导航测试
   - 屏幕阅读器测试

3. **问题修复**
   - 修复验收发现的问题
   - 优化细节体验

### 9.3 风险与注意事项

**风险**：
- 字体文件加载失败 → 回退字体保证可读性
- 圆角过大影响小尺寸卡片 → 小卡片使用 12px–16px
- 品牌紫对比度不足 → 调整深浅版本保证可访问性

**注意事项**：
- 不修改 TypeScript/TSX 逻辑代码
- 仅修改 CSS Module 文件
- 保持现有 CSS 类名和结构
- 不引入新的 UI 框架或库

---

## 十、附录

### 10.1 色彩参考

**品牌紫色阶**（基于 `#9A6ABA` 生成）：

| 名称 | Hex | RGB | 用途 |
|------|-----|-----|------|
| Purple 50 | `#F5F0F9` | rgb(245, 240, 249) | 浅背景 |
| Purple 100 | `#E8DDF3` | rgb(232, 221, 243) | 悬停背景 |
| Purple 500 | `#9A6ABA` | rgb(154, 106, 186) | 主色 |
| Purple 600 | `#8759A8` | rgb(135, 89, 168) | 悬停态 |
| Purple 700 | `#734D96` | rgb(115, 77, 150) | 按下态 |

### 10.2 间距系统

**8px 基准系统**：

| Token | 值 | 用途 |
|-------|---|------|
| `space-1` | 4px | 紧密间距 |
| `space-2` | 8px | 最小间距 |
| `space-3` | 12px | 小间距 |
| `space-4` | 16px | 标准间距 |
| `space-6` | 24px | 中等间距 |
| `space-8` | 32px | 大间距 |
| `space-12` | 48px | 区域间距 |

### 10.3 阴影系统

**实体材质阴影**（无模糊）：

```css
/* 卡片阴影 - 层级 1 */
box-shadow: 0 1px 0 0 rgba(0, 0, 0, 0.04);

/* 卡片阴影 - 层级 2 */
box-shadow: 0 2px 0 0 rgba(0, 0, 0, 0.06);

/* 悬浮阴影 */
box-shadow: 0 4px 0 0 rgba(0, 0, 0, 0.08);
```

### 10.4 参考资源

- **OneSeven 品牌指导**：`.agents/skills/yiqi-ui-standard/references/oneseven-guidance.md`
- **产品画像**：`.agents/skills/yiqi-ui-standard/references/product-archetypes.md`
- **视觉系统**：`.agents/skills/yiqi-ui-standard/references/visual-system.md`
- **字体文件**：`.agents/skills/yiqi-ui-standard/assets/fonts/`
- **Logo 资源**：`.agents/skills/yiqi-ui-standard/assets/oneseven-logo.png`

---

**文档状态**：✅ 已完成，待用户批准

**下一步**：用户审阅本规范，确认后进入代码实现阶段
